-- ==============================================================================
--  Virtual☆Paradise — Autostart Configuration
-- ==============================================================================
--  Default to live video wallpaper; auto-fallback to static Miku wallpaper if unavailable
o.launch_on_start("~/.local/bin/toggle_live_wallpaper.sh init")
